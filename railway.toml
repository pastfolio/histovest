[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "cd artifacts/histovest/backend && python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
